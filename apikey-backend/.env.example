# Copie ce fichier en ".env" (sans .example) et adapte si besoin.
# Valeurs par défaut de Laragon : host=localhost, user=root, mot de passe vide.

PORT=5000

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=apikey_db

JWT_SECRET=change_moi_en_une_longue_chaine_secrete
JWT_EXPIRES_IN=7d
